# CLAUDE.md

This repository uses an agent-agnostic harness. The source of truth is **[AGENTS.md](./AGENTS.md)** —
read it first. Everything an agent needs (the map, the commands, the principles, the protected zones,
how to verify) is there or in the docs it indexes.

This file exists only as an adapter for Claude Code. Do not duplicate rules here — if something matters,
it goes in `AGENTS.md` or `docs/`, where every agent (Claude, Codex, Cursor) sees it.
