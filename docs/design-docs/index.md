# docs/design-docs — index

The documents that define HOW work is done in this repo (the "why" behind the harness). Read in order on
your first time here; afterward go straight to what the task needs.

- **`core-beliefs.md`** — the 10 agent-first principles governing every decision. Start here.
- **`golden-principles.md`** (in `docs/`) — the mechanical code rules the linters enforce.
- **`evolving-the-harness.md`** — the meta-loop: when and how to make the harness GROW (friction → new piece).
- **`doc-as-living-system.md`** — how documentation stays alive (indexes, generated docs, doc-gardening).
- **`00-EXTRACT-REALITY.md`** — the prompt that extracts a code repo's reality (application step).
